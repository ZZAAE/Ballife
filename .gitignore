node_modules/

# docker-compose 비밀값 (.env.example 은 허용)
.env

# 인증키 들어있는 설정 yml 만 차단 (.example / application-prod.yml 등은 허용)
#  ※ **/*.yml 처럼 전체 yml 을 막으면 워크플로·compose·application-prod.yml 까지
#    커밋이 안 되니 절대 금지.
**/application.yml
**/application-local.yml
!**/application.yml.example
